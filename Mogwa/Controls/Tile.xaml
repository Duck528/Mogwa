<Page
    x:Class="Mogwa.Controls.Tile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mogwa.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="720" Height="240"
    mc:Ignorable="d">

    <StackPanel Orientation="Horizontal">
        <StackPanel.Children>
            <Image Source="{Binding imgPath}" Width="240" Height="240"/>
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="{Binding Singer}" FontSize="28"/>
                <TextBlock Text="{Binding Album}" FontSize="28"/>
            </StackPanel>
        </StackPanel.Children>
    </StackPanel>
</Page>
